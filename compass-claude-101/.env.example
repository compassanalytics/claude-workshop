# Compass Claude 101 — example environment.
# Copy to .env (which is gitignored). The block-secrets hook will refuse
# to commit any file matching .env, .key, .pem, etc.

DATABASE_URL=sqlite:///./compass.db
JWT_SECRET=change-me-in-prod
GITHUB_PAT=ghp_replace_with_your_token
LOG_LEVEL=INFO
